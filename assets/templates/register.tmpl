<!DOCTYPE html>
<html lang="en">
<head>
    <title>Panurge</title>
    {{template "header"}}
</head>
<body>

<form class="form" method="POST" action="/register">
    <input type="text" placeholder="Username"
           pattern="[a-zA-Z0-9]{3,15}" title="username must only contain alphanumeric characters"
           name="username" required
    >
    <input type="password" pattern="([a-zA-Z0-9!#$%^*?,./\\_]){8,}"
           placeholder="password" name="password" required
    >
    <p>use at least one uppercase letter, one numeral and 8 charcters. \n Accepted special chararcetrs: !#$%^*?,./\_</p>
    <input type="email" placeholder="email" name="email" required>
    <input type="submit" value="Login">
</form>
{{if .Error}}
<p style="color:red">{{.Error}}</p>
{{end}}
{{template "footer"}}
</body>
</html>